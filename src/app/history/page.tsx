import { useEffect } from "react";

const HowToPage: React.FC = () => {
    useEffect(() => {
        console.log("bing bom bam")
        //if reference search params exist, verify transaction
    }, [])
    return (
        <div>ORDER HISTORY PAGE</div>
    );
}